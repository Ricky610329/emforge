# 2026-09-07 全面檢查（第二輪 M12–M16 之後）

> 對象：HEAD `63260ca`（476 測試綠，其中 6 條預設 skip）。方法：六個面向各一位獨立審查員（唯讀），每條發現由彙整者用審查員留下的重現腳本或原始碼位置**逐條查證**後才收錄；已在 `implementation.md` §12 記錄的限制不重列。
> 狀態：**只檢查、未修**。修復順序建議見文末。嚴重度：S1＝資料損壞／量測錯／安全邊界失效／整台停擺；S2＝功能錯但可繞、狀態不一致可自癒；S3＝可維護性／文件不符／小瑕疵。

| 面向 | S1 | S2 | S3 |
|---|---|---|---|
| A Depot 與資料層 | 0 | 5 | 5 |
| B 佇列／runtime／worker 狀態機 | 0 | 4 | 3 |
| C 儀器層 | 1 | 8 | 3 |
| D MCP／認證／CLI／部署 | （同 C 的 S1） | 7 | 4 |
| E 文件對帳 | 0 | 0 | 3 |
| F 測試品質 | 0 | 4 | 5 |
| 去重後合計 | **1** | **24** | **20** |

三方獨立命中：doctor 探針只綁 pid（A／C／D）。雙方命中：租約重入（C／D）、confirm token 同窗同值（C／D）、`EMFORGE_ROOT` 設在 NAS（C／D）、MemoryDepot 註冊表（A／F）。

---

## S1

### 1. 儀器租約「同 owner 可重入」＋ `by` 有預設值 → 同機兩個 HFSS session，先開的永不關
- `emforge/device/instrument.py:124`（`acquire`）、`:278`（owner＝`f"mcp:{by}"`）、`emforge/device/mcp_server.py:154`（`by="mcp"`）、`emforge/cli/device.py:167`（`--by` 預設 `cli`）
- 兩個呼叫者都不帶 `by`（LLM 一輪並行兩個 tool call 是常態）→ owner 相同 → 雙雙進 `_run_adhoc`，後者 `open()` 覆寫 `self.sim`，先跑完的 `finally: close(); release()` 關的是對方的、放的是對方的租約；先開的 HFSS 沒人 close。真 socket 上兩筆都 `status=done`、`lease_refused=0`；worker 還會在另一筆跑到一半時 `acquire("queue:…")` 成功。不同 profile 併發時結果檔的 `profile_hash` 與實際量的幾何對不上。
- 修：`simulate_once` 用一次性 owner（`mcp:{by}:{uuid}`），重入只留給 worker 的 `queue:<store>`；或 `acquire` 一律「有持有者就拒」。
- 已驗證（`scratchpad/reviewC/t2_lease.py`；D 在 uvicorn 真 socket 重現）。

## S2（依影響排序）

### 2. `abandon` 的守門被 `.fail` 遮蔽 → 對還在量的批標 done，量測結果永久丟失
- `emforge/runtime/collect.py:121`（`state(store)=="claimed"`）、`emforge/queue.py:69`（`state()` 順序 done→fail→claimed）
- 218 卡在 open 重試 → claim 陳舊 → 216 接管正在跑；218 最後 `mark_fail` 寫 `.fail`（`release` 動不了 216 的 claim）。此刻 `.fail` 與新鮮 claim 並存，`state()` 回 `fail` → `abandon` 放行 → inflight 刪掉、佇列 `done`，216 之後每一筆結果沒有 inflight 可收。`requeue` 用 `is_live()` 判得對，`abandon` 用了另一把尺；§12-14 還叫人「沒人接就 abandon」。
- 修：`abandon` 改用 `queue.is_live(store)`；或 `state()` 把新鮮 claim 提到 fail 之前。
- 已驗證（`scratchpad/B/repro_abandon.py`）。

### 3. 公證重測被當成獨立 blind 樣本 → P(勝 blind) 與 k_min 閘被汙染（研究判決層級）
- `emforge/report.py:52,58`（只看 `arm`，不看 `kind`）；成因 `runtime/notarize.py:59`（重測沿用 `arm=rec.arm`、`kind=repeat`）
- 一筆 blind 破榜 → 自動重測 ×3 → 三筆 `arm=blind, kind=repeat` 進參考分佈頂端、`blind_n` 灌水過 k_min。實測 25+25 樣本：公證前 P=0.52，公證後 P=0.469，零新增獨立樣本、判決翻盤。
- 修：`blind_count`／`strategy_rows` 排除 `kind==repeat`（或同 id 取保守值再統計）；`tests/test_report.py` 釘「重測不改 P」。
- 已驗證（`scratchpad/depot/exp10_report.py`）。

### 4. `Database.refresh`／`load` 不承受「列到但讀不到」→ 一個壞 `.npz` 讓 `emforge run` 起不來；真清空時索引永不壓實
- `emforge/db.py:88-94,110-111`；觸發點 `runtime/core.py:237`（取鎖後、reconcile 前）
- 部分複製的備份、備份軟體獨佔、S3 型最終一致、或人剛刪掉 → `_load_npz` 直接拋 → runtime 帶 traceback 死。「列舉空不壓實」的保護在真的清空時讓 `View.top(1)` 一樣炸。
- 修：載入迴圈與 `load` 對讀不到／解不開的 stem 跳過（可發事件），不讓單一壞檔停掉實例。
- 已驗證（`scratchpad/depot/exp4_db.py`、`exp9_misc.py`）。

### 5. 心跳一次讀不到鎖檔＝永久 `lock_lost` → runtime 整夜停擺
- `emforge/runtime/core.py:79-89`、`emforge/fs.py:148`（`read_claim` 只吞 FileNotFoundError、不對 PermissionError 退避）
- `heartbeat` 用 `owner(key) is None` 判「被別人拿走」；契約說缺／空／半截都回 None；SMB 瞬斷的 winerror 53/67 在 CPython 是 FileNotFoundError → 一次讀不到就立不可逆 `_lost`。`heartbeat.py` docstring 與測試都寫「例外＝不知道、不立 lost」，這條被 None 繞過；§12-16 描述不符。
- 修：對 None 先重試確認（或 `touch` 成功就當活著），只有讀到**別人的** owner 才立 lost；`read_claim` 比照 `read_json` 對 PermissionError 退避。
- 已驗證（`scratchpad/B/repro_blip.py` A 段）。

### 6. 急停落在 pick 之後、open 之前 → 被當機器故障重試三次、寫進 `.fail`、這台從此不撿這批
- `emforge/worker/batch.py:61,177`（`open_with_retries` 未帶 `fatal=`）
- `Instrument.open()` 先查急停拋 `EstopEngaged`，`open_with_retries` 當「卡住」重試 3×15 s → `SimulatorOpenFailed` → `mark_fail`。事件是 `job_failed` 不是文件 §6 的 `job_yield reason=estop_engaged`；本機進 `.fail` 名單。保險絲冷卻後的 `_restart` 也走同一條，正是人最可能按急停的時刻。同一個缺口讓第 8 條的假 `PreconditionFailed` 也被重試三次。
- 修：兩處 `open_with_retries(..., fatal=(EstopEngaged, PreconditionFailed))`，`EstopEngaged` 對映 `_yield(run, "estop_engaged")`。
- 已驗證（`scratchpad/B/repro_estop_fail.py`）。

### 7. worker 主迴圈與 runtime tick 對 depot 例外沒有故障邊界 → 一次瞬斷＝行程結束，且沒有 supervisor
- `emforge/worker/loop.py:85-98,117`（pick／log／release 在 try 外）、`emforge/runtime/core.py:245-258`（`tick()` 無 except）
- review-6 的邊界只包住 gate 之後。`Queue.pick` 的 `FsBusy`／`FsCorrupt`／非 FileNotFoundError 的 OSError（winerror 64/59/1231）直接穿出 → exit 1；`start_worker.cmd` 與 `emforge run` 都是一次性，沒有重啟迴圈。
- 修：`_loop` 一整圈與 `Runtime.run` 的 `tick()` 各包一層：記事件、睡一個 poll 後續跑；或 `start_worker.cmd` 加重啟迴圈。
- 已驗證（`scratchpad/B/repro_blip.py` B 段）。

### 8. `doctor.probe_root` 探針檔只綁 pid → 殘留會永久擋住同 pid 的 `open()`；同行程並發互相誤報「root 不可寫」
- `emforge/doctor.py:35-44`；呼叫端 `device/instrument.py:189,260`、`device/reference.py:37`
- 殘留：Ctrl-C 落在 claim 與 release 之間、或 `fs.release` 拋 `FsBusy`（不是 OSError、逃出 except）→ `.doctor_probe_<pid>` 留著 → 之後同 pid 每次 `open()` 前置檢查都不過，沒有自我修復；Windows pid 會重用。並發：`worker --serve` 下主執行緒、心跳（每小時刷說明檔）、SDK thread pool（`device_describe`／`device_selfcheck`）同時叫 `health()` → 8 執行緒 160 次失敗 126 次；真 MCP 並行叫兩個唯讀 tool 六次六次命中；`simulate_once` 走 `fatal=` 立刻 `precondition_failed`，訊息指向錯方向。跨機：三台共用 NAS root＋pid 重號 → `start_worker.cmd` 的 doctor 閘會拒起健康機器。`FileDepot.selfcheck` 的探針帶 `<pid>.<rand>`、沒這問題。
- 修：探針名加 tag＋uuid、`finally` 清、`except` 接 `FsBusy`；或直接用 `depot.selfcheck()`，別維護第二套探針。
- 已驗證（`scratchpad/depot/exp9_misc.py` C 段、`reviewC/t3_misc.py` [6]）。

### 9. confirm token 同窗同值＋單次使用 → 同一件事十分鐘內做不了第二次，錯誤訊息指向走不通的路
- `emforge/device/limits.py:80-95`、`device/instrument.py:146-156,275,289`、`device/mcp_server.py:178-188`
- token＝`sha1(secret|op|key|10 分鐘窗)[:8]`，同 `(op,key)` 同窗永遠同值；成功一次即燒掉 → 重測同一片／`stop→resume→stop` 都被拒，訊息「先不帶 confirm 拿新 token」拿回的是同一個。`deploy.md` §6.4 沿用 §6.3 的王 pattern 會得到 `confirm_rejected` 而不是 `device_busy`，驗收會誤判。
- 修：token 加 nonce（`issue_confirm` 存 `{token: (op,key,expire)}`，每次不同、消費即失效）；或至少區分「用過」與「不對」並改訊息。
- 已驗證（`scratchpad/reviewC/t1_confirm.py`；D 在 MCP 層重現）。

### 10. `deploy.md` 把 `EMFORGE_ROOT` 設在 NAS → 「本機層」急停與 `limits.json` 三台共用，NAS 斷線時三層急停全部 fail-open
- `docs/deploy.md:32,35`、`emforge/paths.py:297,302`（`estop_local`／`limits_json` 掛在 root）、`estop.py:5`（「NAS 斷線也擋得住」）
- 三台 `EMFORGE_ROOT` 同一個 NAS 路徑、不設 `EMFORGE_DEPOT` → `device-estop engage --local` 三台一起停、`clear_local` 一次解三台；每台自己的 `limits.json` 做不到；NAS 斷線時 `exists` 吞 OSError 回 False → 三層都讀成「沒有急停」，`load_limits` 讀不到檔靜默回預設＝全部 profile 放行。與 §13「`--root` 本機程式碼／`--depot` 共享狀態」的設計自相矛盾。
- 修：`deploy.md` 每台改本機 `EMFORGE_ROOT`（放 registry.py／limits.json／ESTOP）＋ `EMFORGE_DEPOT=file://T:\…`；或把 ESTOP／limits 改成獨立本機路徑（`EMFORGE_LOCAL`／`%LOCALAPPDATA%`），並在 NAS 讀不到時把急停判成 engaged（fail-closed）。
- 已驗證（code 路徑；C／D 雙方獨立）。

### 11. 策略拿到的 `View` 一個屬性就反手可寫，而且那個 `Database` 沒綁 profile
- `emforge/db.py:124-128`（`View._db`）、`emforge/strategy.py:185`（`Database(depot)` 未給 `write_profile`）
- D7 宣稱「View 結構上沒有寫入方法」，實際 `ctx.db._db.add(...)` 可寫任何 profile；一筆從未碰 HFSS 的 `status=done, score=9.99` 可以落地被 `report`／`top()` 當真。`refresh` 的 `append`／`rewrite_log` 也不過 `write_profile` 守門。
- 修：`strategy.py:185` 傳 `write_profile=profile.name`；`View` 只持讀取用閉包／索引快照，不持 `Database`。
- 已驗證（`scratchpad/depot/exp9_misc.py` A／B 段）。

### 12. `Depot.check_prefix` 不擋 `..`／`.`／空段 → `list`／`ensure_prefixes` 可逃出 root
- `emforge/depot/base.py:36-41`（與 `check_key` 不對稱）
- `list("../outside/")` 列出 root 外檔案；`ensure_prefixes(["../pwned/deep/"])` 在 root 外建目錄；同字串給 `newest()` 卻炸。今天所有 prefix 都來自 `paths.py`，所以是圍籬破洞非現行漏洞。
- 修：`check_prefix` 套用與 `check_key` 相同的段檢查；契約測試補一條。
- 已驗證（`scratchpad/depot/exp1_prefix.py`）。

### 13. MCP 埠被佔時 daemon thread 靜默死亡，但狀態字典 `url` 與 `device_serve` 事件已宣告成功
- `emforge/device/mcp_server.py:260-274`（`announce_url`／`log` 在綁定之前）、`cli/loops.py:63-64`（不看 `t.is_alive()`）
- uvicorn `sys.exit(3)` 在非主執行緒被 `threading.excepthook` 靜默吞掉；worker 照跑；`fleet --mcp-config` 吐一個連不上的端點。`device-serve` 撞埠則以 3（＝`EXIT_LOCKED`）退出、`main()` 接不到 `SystemExit`。對照：`check_bind` 拒起時整個 worker 不啟動——兩種失敗處置不對稱。
- 修：先 `bind_socket` 成功再 announce／log；`serve_in_thread` 回傳前確認執行緒活著，否則清 `url` 並以非零退出或明顯警告。
- 已驗證（D 真 socket；code 位置確認）。

### 14. `EMFORGE_MCP_PORT` 空字串或非數字 → 每一個 emforge 命令都吐 traceback（含 `start_worker.cmd` 的 doctor 閘）
- `emforge/cli/loops.py:41`（建 parser 時 `int(os.environ…)`）、`cli/__init__.py:33`（`parse_args()` 在 try 外）
- `EMFORGE_MCP_PORT=""`（PowerShell `$env:X=""`、`setx X ""`）→ `version`／`--help`／`doctor` 全掛；`start_worker.cmd` 誤報成「doctor 有阻擋條件」。
- 修：`default=os.environ.get(...) or DEFAULT_PORT` 交給 argparse 轉；或把 `parse_args` 包進 try。
- 已驗證（`EMFORGE_MCP_PORT="" python -m emforge version`）。

### 15. 急停中 `worker --once` 永不返回
- `emforge/worker/loop.py:88`（急停分支 `continue`，沒看 `once`）
- `emforge worker --once` 與 `testing.run_all_jobs` 在急停時每 30 s 睡一次、永遠不退出、不印訊息。
- 修：急停分支 `if once: log("worker_stop", reason="estop"); return`。
- 已驗證（`scratchpad/reviewC/t4_once.py`）。

### 16. `limits.json` 只驗 JSON、不驗型別 → 少一對中括號＝所有 profile 都不准；字串數字＝整批判死
- `emforge/device/limits.py:26,33,67`
- `{"allowed_profiles": "dual_p01_db075"}` → `tuple(str)` 逐字元 → 全部擋，訊息列出 `['d','u','a',…]`；`{"max_sample_s": "1800"}` → TypeError 被當機器卡住重試三次後判死整批。
- 修：`from_dict` 加型別／範圍檢查，不合就跟壞 JSON 一樣指名檔案拒起。
- 已驗證（`scratchpad/reviewC/t3_misc.py` [5]）。

### 17. `_restart` 對 `Instrument` 的 close→kill 順序讓 kill 打空（殺不透）
- `emforge/worker/batch.py:170`、`device/instrument.py:230,237`（`close` 先把 `sim` 設 None，`kill` 讀 `sim`）
- retry_pass／fuse_cooldown／watchdog_timeout 三個重開點在儀器路徑上少了「殺透」；`_opened_once` 之後前置檢查又不再把 ansysedt 在跑當阻擋，殘留行程累積沒人抓。現有測試只在裸模擬器路徑斷言 kill。
- 修：`_restart` 改 kill→close→open，或 `close()` 保留 `_last_sim` 供 kill；補儀器路徑的 watchdog→重開回歸測試。
- 已驗證（`scratchpad/reviewC/t3_misc.py` [3]）。

### 18. `close()`／`quit()` 沒有處決線：COM 卡住就永遠卡在 `finally`，租約永不釋放
- `emforge/worker/batch.py:180`（`_close_quiet`）、`device/instrument.py:236,285`（`simulate_once` 的 `finally: close()` 在 `release` 之前）、`adapters/antenna/sim.py:141`
- `guard.py` 的疤寫「每個碰模擬器的環節都要有處決線」，但只有 open／simulate 包了 `guarded_call`；`quit()` 不回來時 worker 主迴圈永遠卡住（裝置心跳還在寫、`fleet` 顯示線上），`simulate_once` 則租約永不放、worker 每輪 `device_busy` 讓位。
- 修：close 改 `guarded_call(sim.close, CLOSE_TIMEOUT_S, sim.kill)`；`simulate_once` 的 `finally` 先 `release` 再 `close`。
- 信心中：機制與缺口確定，「quit 會卡」是舊系統既有失效模式、未在此 repo 實測。

### 19. `min_free_gb` 量的是 `default_work_root()`，不是儀器實際用的 `--work-root`
- `emforge/doctor.py:58`、`device/limits.py:71`、`device/instrument.py:61`
- `worker --work-root D:\scratch` 時 HFSS 寫 D:，I-1 的磁碟守門量的是 `%LOCALAPPDATA%` 那顆；`reference.md` 也標錯目錄。
- 修：`health(root, *, depot=None, work_root=None)`，`Instrument.open()` 傳 `self.work.root`。

### 20. 機器 tag 靠 UDP 路由探測取 IP 末段 → VPN／多網卡／DHCP 會靜默換身分，單機急停與 STOP 隨之失效
- `emforge/netid.py:9-24`、`cli/loops.py:52`、`scripts/start_worker.cmd:16`（沒釘 `EMFORGE_MACHINE`）
- 本機實測有兩個位址（140.123.x 與 100.x CGNAT 段）；任何拿到 10/8 路由的介面會搶走答案。tag 一變：`queue/ESTOP.<tag>`／`STOP.<tag>` 對不上（已按下的單機急停失效）、`devices/` 分岔、舊 claim 只能等 stale。離線時回 `127.0.0.1` → tag `1`，多台撞名。
- 修：`deploy.md` §2 每台 `setx EMFORGE_MACHINE <tag>`，`start_worker.cmd` 沒設就拒起；`doctor` 對 `local_tag()` 與 `devices/` 清單不符時警告。

### 21. `device-estop` 同給 `--tag` 與 `--local` 時靜默忽略 `--tag`；`clear` 沒東西可清也回 0
- `emforge/cli/device.py:92,96,103`
- 從開發機 `engage --tag 216 --local` 只在開發機自己的 `<root>/ESTOP` 建檔、回 0、印「已按下（本機）」，216 照跑。這是唯一能按下／解除安全互鎖的路徑。
- 修：`--tag`／`--local` 互斥組；`clear` 在「本來就沒有」時回非零。
- 已驗證（D 實跑）。

### 22.（測試）正式路徑 `propose_in_subprocess(depot=…)` 零覆蓋
- `tests/runtime/conftest.py:33`、`tests/test_e2e.py:95,125,147`、`tests/test_cli.py:78-110`（全注入 in-process）；`tests/test_strategy.py:160-188` 五處都不帶 `depot=`
- 正式機每個 tick 走的「子行程＋`--depot file://T:/碩二…`」在 476 條裡一次沒踩到。F 另寫探針實測目前正常（中文路徑、跨樹 OK），所以是假綠面積不是現行 bug。
- 修：`test_strategy` 加 `propose_in_subprocess(..., depot=FileDepot(root))`；runtime 測試至少一條走預設 `propose_fn=None`。

### 23.（測試）測試耦合「這台機器現在的狀態」：ansysedt 在跑時 31 條失敗＋1 條掛死
- `emforge/doctor.py:20,52` → `limits.check_preconditions` → `Instrument.open()`；只有 6 處測試各自 monkeypatch，`tests/device/conftest.py`、`tests/worker/*`、`tests/runtime/*`、`test_e2e.py` 都沒有
- 正式機（ansysedt 常駐）或開發機開著 HFSS 跑測試 → `health()["blocking"]` 非空 → 31 failed；free_gb < 5 GB、時鐘偏移 > 30 s 同理。這也是 M16 那次「未重現 1 failed」最合理的來源（F 的計時型嫌疑全部重跑 20 次零翻、餘裕 2.2×）。
- 修：`tests/conftest.py` autouse 把 `doctor.ansysedt_running` 釘 False、`_free_gb` 注入。

### 24.（測試）`test_loop_stop_file_finishes_current_job_then_exits` 沒有上限：任何 open 失敗變成整套掛死
- `tests/worker/test_loop.py:57-74`（`once=False`、`sleep=lambda s: None`、唯一停止來源在 `simulate()` 內）
- open 失敗 → simulate 不被叫 → 批 fail → pick 回 None → 無 sleep 忙迴圈永不返回；沒裝 pytest-timeout。F 實測 100% 單核 217 s 後手動 kill。
- 修：`sleep` 改計數器，第 N 次 `request_stop` 並 assert 迴圈次數。

### 25.（測試）conftest 只 pop `EMFORGE_ROOT`，沒 pop `EMFORGE_DEPOT` → 設了就把假資料灌進共享 depot
- `tests/conftest.py:14-19` vs `cli/base.py:29`
- `EMFORGE_DEPOT=file://<某樹>` 下跑 `tests/test_cli.py` → 19 failed，並在那棵樹寫出 72 個檔（`db/fake_f1/*.npz` 21 筆、batches、queue）。
- 修：`_no_nas_env` pop 整組 `EMFORGE_*`（至少 DEPOT／WORK／MACHINE），session 結束還原。

## S3

26. **`emforge run` 對帳不符 exit 2 但終端全靜默**（`runtime/core.py:240-244`；`reconcile.py:3` 自稱「印差異」）。修：`return 2` 前 `err(...)`。
27. **`propose_in_subprocess` 遇 `memory://` 在子行程開出一個空的同名 depot**（`strategy.py:230`、`depot/uri.py:29`）：只有 `cmd_run` 有防護；直接建 `Runtime(depot=MemoryDepot())` 的呼叫端會讓策略靜默看到空庫。修：子行程模式對未註冊 `memory://` 拋。
28. **`Queue._claim` 判 "mine" 不刷心跳；接管已全部跑完的批仍先開 HFSS**（`queue.py:145-147`、`worker/batch.py:61-64`）。修：mine 時 `touch`；`_todo` 為空直接回 done。
29. **`check_key` 不擋磁碟機代號**（`depot/base.py:27-34`）：`"C:/Windows/x"` 通過、`Path(root)/key` 取代 anchor、實測寫到 root 外。修：任一段含 `:` 即拒。
30. **MemoryDepot 與 FileDepot 三處語義分歧、契約沒釘**（`memory.py:48-57,85-89`）：log key 的 `get_bytes` File 回 bytes／Memory 回 None；`delete` 的 `or` 短路只刪 doc；`set_modified_at` 缺 key File 拋／Memory 靜默。修：統一並補三條契約測試。
31. **MemoryDepot 註冊表永不釋放、同名重建靜默覆蓋、匿名也註冊**（`memory.py:14-17,33-34`）：同行程連跑測試第二輪拿到第一輪的 depot（`pytest.main` 兩次 → `assert 2 == 1`），擋住 in-process flake 獵捕；`tests/conftest._fresh_registries` 只清 specs／profiles。修：`WeakValueDictionary`／同名拋／匿名不註冊；conftest 一併清。
32. **`file://host/share` 被當相對路徑而非 UNC**（`depot/uri.py:33-38`）：打錯一個斜線 worker「正常啟動、什麼都撿不到」並在 cwd 下建樹。修：非磁碟機字母的 authority 轉 `//host/…` 或拒絕並提示 `file:////`。
33. **`fs.py` 四個零呼叫函式（`atomic_write_json`／`touch`／`is_stale`／`newest_mtime`）；`Ledger.promote`／`rescore` 無鎖 read-modify-write**（lost update，checksum 抓不到）。修：刪死碼；promote／rescore 包 `depot.lock`。
34. **`estop_cleared` 事件的 `scope` 永遠 None**（`device/instrument.py:166-167`：先 `_set(estop=None)` 再讀）。修：先存 scope 再 `_set`。
35. **`device-estop clear` 只清一層卻印「已解除」**（`cli/device.py:103-104`）：全機／本機層仍在時機器照停。修：clear 後再 `engaged()`，仍有就印警告回非零。
36. **急停解除用 `sim is not None` 推回 `ready`，被 abort／看門狗殺過的 sim 仍非 None → 狀態字典說 ready 但 HFSS 已死；`device_abort` 的錯誤字串不觸發 `_restart`**，後續樣本撞到保險絲才重開（約賠 4 筆＋600 s）。修：kill 後標 `_sim_dead`；abort 走與 estop 相同路徑。
37. **401 在客戶端呈現成 `ExceptionGroup: unhandled errors in a TaskGroup`**（`device/mcp_client.py:17-29`）：看不出是 token 問題，而伺服器端有人話。修：攤平 `ExceptionGroup`，401 改印「檢查 EMFORGE_DEVICE_TOKEN」。
38. **`parse_bits` 對非數值 list 回 `internal:` 不是 `bad_bits:`**（`mcp_server.py:88-94` 只接 ValueError）。修：接 `(ValueError, TypeError)`、先 `asarray(dtype=float)`。
39. **`DeviceCallFailed` 訊息帶 SDK 前綴 `Error executing tool <name>: `**（`mcp_client.py:3` docstring 與 §8 說「原文就是錯誤碼」）。修：剝前綴或改文件用子字串比對。
40. **三個小瑕疵**：`call_device` 的 `timeout_s` 也綁 connect（防火牆黑洞卡一小時，建議 `connect=10`）；`device_log(n)` 無上下限、`n≤0` 靜默變 1；`pyproject` 的 `mcp` extra 沒直接宣告 `httpx2`／`uvicorn`（靠 mcp 傳遞），`mcp_client.py` docstring 說 import 了 `anyio` 但沒有。
41. **`device/limits.py:62-66` 的 `elif is_retired` 永遠不可達**（`gate.check` 已先擋）。修：刪或改 `if` 並註明。
42. **`CLAUDE.md:38` 的 `hfss` 標記與 `EMFORGE_HFSS_TESTS` 在 code 裡不存在**；adapter 測試用 `EMFORGE_ANTENNA_REPO` skip。修：改寫成現況。
43. **`cli/__init__.py:8`、`cli/device.py:1` 檔頭漏列 `device-serve`／`device-simulate`**。
44. **「476 綠」＝470 passed＋6 skipped**：adapter 綁定／COM 建構那 6 條沒設 `EMFORGE_ANTENNA_REPO` 就靜默 skip。修：文件釘住或 smoke 檢查 skip 數。
45. **`Queue._lock_owner`＝`hostname:pid` 非每實例獨一**（`queue.py:43`；runtime 的是 `tag:pid:rand`）：同行程兩個 `Queue` 同 owner，A 卡 NAS 超過 180 s 被 B 破鎖重認領後，A 的 `release(owner=)` 會刪掉 B 的活鎖。觸發窗窄。修：加 `urandom` 後綴；契約補 `lock()` 層「破鎖後重認領、原持有者離開不刪新鎖」。
46. **其餘覆蓋缺口**：`worker --serve`／`device-serve` 只驗參數字典、沒真起過 server；`device-simulate --timeout-s` 沒斷言；e-stop 本機層沒經 `Instrument.open()`／`worker_loop` 走過；`_undo_break` 的 FileExistsError 分支；`lock_lost` 後 inflight 處置沒斷言；`ledger.py:61 where` 零呼叫；`import-legacy` 舊樹側沒用中文路徑。
47. **測試可維護性**：`tests/test_cli.py` 479 行、多條測試各斷言 8 個以上行為；`_batch`／`_job`／`_rec` helper 在三到四個檔各寫一份；`test_mcp_client.py:45` 的 `pytest.raises(Exception)` 過寬。
48. **`import-legacy --verify` 不符時照樣入庫並記已匯入**，exit 2 是事後告知非攔截，重跑要 `--force`（行為不是 bug，但與驗收語氣易誤讀）。

---

## 檢查過且沒問題的（覆蓋範圍摘要）
- Depot：`_undo_break` Windows `FileExistsError` 實測成立；`break_if_stale` mtime 身分檢查在 NTFS 100 ns 粒度下無法誤判；`paths.py` 對 M12 前逐函式逐位元相同；`check_key` 在 FileDepot 每個原語入口都有；delete-pending 在 Win11/NTFS 走 POSIX 語義；`db.add` 去重與大檔效能（21.8 KB／存 1 ms／讀 4 ms）；events 白名單、ledger checksum 範圍。
- 佇列／runtime：`Queue.pick` 三種競賽（雙台同判 takeover、第三台剛認領、`.fail` 被先刪）新鮮 claim 都動不到；`release(store, tag)` 不誤刪接管者；中途死掉每一格終態自癒或有人工出口；`collect` 冪等與部分可見；`pending.jsonl` 單寫者仍成立；子行程 `--depot` 帶中文＋撇號＋空白＋`&` 的 argv 安全；心跳執行緒生命週期與 `finally` 覆蓋；`reload_config` 半截 yaml 只多一個 tick 的窗。
- 儀器層：`simulate_once` 確實不入 db；adhoc 與批結果欄位一致；confirm 窗收本窗＋上一窗；`write_state` 失敗只丟一次 `at`；`read_fleet` 90 s＝三拍；`Limits` 每個欄位都有使用點；`_set` 鎖範圍正確、`kill()` 跨執行緒不 None deref。
- MCP／CLI：`BearerGate` 覆蓋整個 app（GET／DELETE／OPTIONS／HEAD／`.well-known`／`/docs` 全 401）；`check_bind` 判定保守；stateless＋json_response 語義符合 §12-25、重放由 token 單次擋；同步 tool 真的跑 `anyio.to_thread`（limiter 40）；daemon thread 起真 socket 無 signal-handler 問題；急停「只能按不能解」封閉；confirm token 有綁 op；`by` 不會注入日誌；27 個子命令 `--help` 全通、exit code 表相符；`init` 冪等。
- 文件：§2 模組地圖、§3 key 佈局、§8 CLI、§9 事件（39 個雙向一致）、§12／§13 抽查、`naming.md` 例外命名零破例、`deploy.md` 旗標存在、`pyproject` 版本一致。
- 測試：契約測試對 `base.py` 每個 public 方法都有；無空洞斷言；全域註冊表（specs／profiles）有還原；`EMFORGE_WORK` 不會刪到真工作目錄；flake 嫌疑（0.3 s 門檻）20 次零翻、餘裕 2.2×、加 10 個燒機子行程也不變。

## 建議的修復順序
- **P0（先修再上正式機）**：1 租約重入、2 abandon、3 公證重測汙染 P、5 lock_lost、6 急停進 `.fail`（連 8 的重試連帶）、8 探針、9 confirm token、10 部署根目錄、13 埠撞靜默、14 port 環境變數。
- **P1**：4 refresh 容錯、7 故障邊界、11 View 可寫、15 `--once`、16 limits 型別、17 kill 打空、18 close 處決線、19 磁碟量錯碟、20 機器 tag 釘死、21 estop CLI 互斥。
- **P2（測試假綠）**：22–25、31、44、45。
- **P3**：其餘 S3 與 12 的圍籬修補（12 雖是 S2 但今天無可觸發呼叫端，可與 29 一起做）。
