# 第三輪計畫與 Codex 交接包（2026-09-07）

> 本檔＝給實作者（Codex 或任何 agent）的完整任務書。自成一體：環境、規則、契約速查、M17–M20 里程碑、驗收。
> 先讀 `CLAUDE.md`、`docs/naming.md`、`docs/implementation.md`，再照本檔的順序做；一里程碑一 commit；不 push。

> 第二輪（M12 Depot＋M13–M16 儀器層／MCP）已於 2026-09-06 落地；2026-09-07 全面檢查 P0–P2 已修（HEAD `0a0546a`，519 測試）。本檔改寫為第三輪計畫。

## Context

Ricky 2026-09-07 描述他看到的系統形狀：模擬器 cluster（各種接口＋MCP）／算法（含自己的指標追蹤、要能託管多個、各有 log、跑在有運算力的機器）／共用的模擬庫／資料庫（去重、算法自己取、pattern 命名追歷程、自訂取樣）／評估層（同 profile 下在意的指標）／各層 MCP。
對照 emforge 現況，層都在，差三個缺口：**算法只能被 tick 叫、不能自己送批**；**歷程只有一個沒人填的 `parent`、沒有標籤與取樣**；**評估只有單一聚合分數與 P(勝 blind)**。MCP 只覆蓋模擬器。

Ricky 拍板（2026-09-07）：託管＝**收件匣＋client**（propose() 並存）；多指標**現在做**；歷程＝**填 parent＋算法自訂標籤**；MCP 開**資料庫（唯讀）、評估（唯讀）、收件匣（送批）**三層。
原則補充（Ricky）：算法自己在意的指標是算法本體的一部分（退火看溫度、接受率），平台不解釋不打分；平台評估層只信量測。

探索確認的現況（HEAD `0a0546a`）：`Record.measure` 已是完整 `dict[str, float]`（`specs.measure()` 回的全部軸），`Spec.score = min(measure[axis]+offset)`；`Proposal` 鍵集嚴格 `{pattern, parent, arm, note}`；索引 `INDEX_FIELDS = (id, sim_profile, status, score, strategy, arm, parent, tick, kind)`＋`stem/store/worker_ver`；`View` 有 `query(strategy, arm, status, since_tick, limit)`／`top`／`mine`／`measurements`，沒有 `metas`；strategies.yaml 的預算欄位叫 `batch`；`schedule` 每 tick 依 prio 叫 `propose_in_subprocess`，`dispatch` 先寫 inflight 再寫 batch 再 `queue.add`，去重＝db done ids ∪ inflight ids；`report.strategy_rows(db, profile)` 吃索引 metas。

---

## 給 Codex 的交接包（自成一體，不需要對話脈絡）

### 環境
- Repo：`C:\Users\ricky\Documents\GitHub\emforge`（獨立 git，HEAD `0a0546a`，40 commit，519 測試綠，工作樹乾淨，**無 remote、不 push**）。
- Python：`C:\Users\ricky\miniforge3\envs\ant\python.exe`（3.11；已裝 numpy、pyyaml、pytest、pyflakes、mcp 2.1.1）。
- 測試（一定從 repo 根；`tail` 會吃掉 exit code，所以要 pipefail）：
  ```bash
  cd /c/Users/ricky/Documents/GitHub/emforge && export PYTHONIOENCODING=utf-8 EMFORGE_ANTENNA_REPO=/c/Users/ricky/Documents/GitHub/Antenna && set -o pipefail && /c/Users/ricky/miniforge3/envs/ant/python.exe -m pytest -o addopts="" -q 2>&1 | tail -3 && /c/Users/ricky/miniforge3/envs/ant/python.exe -m pyflakes emforge tests && echo OK
  ```
  改小範圍先跑單檔；整套只在 commit 前跑。
- 先讀：`CLAUDE.md`、`docs/naming.md`、`docs/implementation.md`（§2 模組地圖、§3 key 佈局、§5 runtime、§7 寫策略、§12 已知失效模式）、`emforge/model.py`、`emforge/paths.py`、`emforge/depot/base.py`、`tests/test_smoke.py`（規範的機器版）。

### 硬規則
- TDD：每條行為先寫紅測試再改 code。一里程碑一 commit；**全綠＋pyflakes 才 commit**；只 `git add` 自己動的檔；不 push。commit 訊息繁體中文、格式 `feat: …（第三輪 M17）`；結尾 trailer 用 Codex 自己的，不用 Claude 的。
- 可維護性由 `tests/test_smoke.py` 釘死：單檔 ≤400 行、單函式 ≤60 行、禁模組名 utils／misc／helpers／common／dedust；核心零 torch／antenna／scipy／matplotlib／pandas／win32com；`mcp`／`httpx2`／`uvicorn` 只准在 `device/mcp_server.py`、`device/mcp_client.py`、新的 `platform/mcp_server.py`、`mcpkit.py` 的**函式內** import；協調狀態只經 `Depot`（DEPOT_ONLY 清單的模組不准 import pathlib／fs、不准 `open(`）——新模組要登記進 smoke 的清單（DEPOT_ONLY／PARTIAL／LOCAL_LAYER 三張），否則 `test_every_core_module_is_classified_for_depot_gate` 會紅。
- 磁碟名／key **只能來自 `paths.py`**；key 是 POSIX 相對字串，前綴以 `/` 結尾；新 key 進 `tests/test_paths.py` 的快照。
- 例外類別不加 `Error` 後綴（`ProposalError` 是唯一例外）；CLI 子命令 kebab-case ↔ `cmd_snake`；環境變數 `EMFORGE_*`。
- 註解／docstring／文件繁體中文；每個里程碑同 commit 更新 `docs/implementation.md`（對應節）與 `docs/naming.md`。
- 不碰 `C:\Users\ricky\Documents\GitHub\Antenna` 與 `T:\`；測試只用 `tmp_path`／`MemoryDepot`。
- Windows：多行 patch 用 Python 腳本或編輯器寫，別用 bash heredoc 夾中文＋引號。

### 現況契約速查（HEAD `0a0546a`，實作時仍以 code 為準）
- `model.py`：`Proposal(pattern, parent=None, arm=None, note={})`，`KEYS` 嚴格鍵集，`from_dict` 多鍵拋 `ProposalError`；`Record(id, sim_profile, bits, response, measure: dict, score, status, strategy, arm, parent, tick, seed, note, kind, run: dict{store,machine,worker_ver,profile_hash,time_s}, extra)`，`META_FIELDS`＝除 bits／response 外；`record_id = sha1(packbits(bits)+profile)[:16]`；`Context(db: View, profile, budget, rng, workdir, tick, params)`；`Spec(name, labels, measure, axes, offsets)`，`score = min(measure[axis]+offset)`，缺軸／NaN → None；常數 `STATUS_*`、`KIND_SAMPLE/REPEAT`、`ARM_BLIND`。
- `db.py`：`INDEX_FIELDS=(id, sim_profile, status, score, strategy, arm, parent, tick, kind)`＋索引行另有 `stem, store, worker_ver`；`Database(depot, write_profile=None)`，`add()` 同 id 同 store 回 False 不覆寫；`Reader`（`metas/ids/load/try_load/measurements/profiles`）；`View`（只持 Reader）：`query(profile, strategy, arm, status, since_tick, limit)`、`top(k)`（每 id 取 min）、`mine()`、`measurements()`；沒有 `metas()`。
- `specs.py`：`register_measure(name, fn, *, labels, targets)`、`register_spec(Spec)`、`measure(name, response, labels) -> dict[str,float]`（回全部軸，`Record.measure` 存的就是這個 dict）、`score(spec, measured)`；內建 `dual_v2`（axes m1–m4）、`single_v1`、`fake_v1`；`ledger.rescore(depot, profile, spec, db, by=, force=)`。
- `strategy.py`：`COMPATIBLE`＋`propose(ctx)`；`validate_proposals(raw, profile, budget)` 規則：list、≤budget、鍵集、shape、0/1、`fixed_on` 全 True，**任一筆錯整批拒**；`make_context(root, profile, name, budget=, seed=, tick=, params=, depot=)`；`propose_in_subprocess(...)`；`parse_strategies_yaml(text)`：頂層 `profile／runtime／strategies`，策略項 `name, prio, batch（預算欄位叫 batch）, max_inflight=1, enabled, seed, propose_timeout_s, params`；`RuntimeConfig` 有 `background_prio=9, repeat_n=2, k_min=20, strategy_error_limit=3` 等。
- `runtime/schedule.py`：每 tick 依 prio 升冪叫策略；bg 判定＝prio ≥ background_prio 且沒有前景 inflight 且佇列空；`runtime/dispatch.py::dispatch(rt, name, proposals, tick=, seed=, prio=, kind=, store=, origin=, machine=)`：去重＝db done ids ∪ inflight ids ∪ 批內；**先寫 inflight**（`{store, strategy, tick, seed, kind, prio, ids, items:{id:{parent,arm,note}}, collected, at}`）再 `Batch.write` 再 `queue.add`；`store_name = f"{profile}-{strategy}-t{tick:05d}"`。
- `runtime/collect.py`：把 inflight items 的 parent／arm／note 帶進 Record；`notarize.py` 重測 `parent=rec.id, kind=repeat`。
- `report.py`：`DEFAULT_K_MIN=20`、`strategy_rows(db, profile, k_min=)`（吃索引 metas）、`p_beats_blind(scores, blind_scores)`、`_blind_reference` 已排除 `kind=repeat`、`report(depot, profiles, cross_profile=, k_min=)`。
- `events.py`：`EVENTS` 是 dict 事件名→必填欄位；`emit(depot, key, event, /, **fields)`；未知事件名拋。
- `paths.py`：`layout_prefixes() = ("db/","ledger/","queue/","queue/state/","queue/log/","batches/","runtime_state/","devices/")`；`RESERVED_STRATEGY_NAMES={repeat, notarize, runtime, cli}`；本機 Path 函式另成一節（`registry_py／user_strategies_dir／strategy_workdir／estop_local／limits_json`）。
- `testing.py`：`FAKE_PROFILE`（fake_f1，shape 8×8，labels L1/L2，n_points 17，measure `fake_m`，spec `fake_v1`）、`FakeSimulator(workdir=, profile=, fail_ids=, hang_ids=, delay_s=)`、`fake_measure → {m1..m4}`、`register_fakes()`、`make_fake_root(root, depot=)`、`run_all_jobs(root, machine_tag=, depot=, **loop_kw)`。runtime 測試慣例：`Runtime(root, profile, depot=MemoryDepot(name=唯一), propose_fn=propose_in_process)`；`memory://` 只認已建好的名字。
- MCP 骨架可重用：`device/mcp_server.py` 的 `BearerGate`／`check_bind`／`http_app`／`serve_in_thread`／`two_step`／錯誤碼轉換；confirm token 是 nonce 式（`Instrument.issue_confirm／check_confirm／consume_confirm`）。

### 交付順序與每步驗收
M17 → M18 → M19 → M20，各一 commit，每步：全套綠＋pyflakes＋smoke 守門＋該里程碑的 docs 節更新。最後把 `docs/review-2026-09-07.md` 不動、`docs/implementation.md` 版本行改測試數。

---

## 四條硬規則（摘要；細節見交接包）
TDD／命名照 `docs/naming.md`、磁碟名只經 `paths.py`／一里程碑一 commit（全綠＋pyflakes；紅的不 commit）／可維護性由 `test_smoke` 釘死。**不 push**；正式機驗收延後；不碰 Antenna repo 與 `T:\`。

---

## M17　評估層：Spec 閘門與聚合、效率曲線、校準

**不改 Record 格式**（measure dict 已存完整指標）；改的是評估規則與報表。

- `model.Spec` 加欄位（向後相容、預設＝今天行為）：`aggregate: str = "min"`（`min`｜`wsum`）、`weights: tuple = ()`（wsum 用）、`gates: tuple = ()`（每條 `(axis, op, value)`，op ∈ `<=`,`>=`；任一不過 → `score=None`，索引 `status` 不變、`gated=True`）。`Spec.score(measure)` 依此算；`specs.register_spec` 驗證欄位。內建 `dual_v2`／`single_v1`／`fake_v1` 不變。
- 索引 `INDEX_FIELDS` 加 `pred`（策略預測分數，來自 `Proposal.note["pred"]`，缺＝None）與 `gated`；舊索引行缺欄位＝None（`from_meta` 容忍）。`collect` 把 `note["pred"]` 帶進 Record 與索引。
- `report.py` 拆：`report.py`（總表，不變）＋新 `emforge/evaluation.py`：
  - `efficiency_curve(db, profile, *, strategy=None, window=None) -> list[(n_done, best_conservative)]`——best-so-far 對累計 done 筆數（同 id 取保守值、排除 `kind=repeat`），全域與每策略；
  - `calibration(db, profile, strategy) -> {n, rho, mae}`——`pred` vs 量測 score（只算兩者皆非 None）；
  - `metric_summary(db, profile, strategy=None) -> {axis: {n, mean, best, gate_pass_rate}}`。
  - CLI：`emforge report --profile P --curve [--strategy S] [--since-tick N]`／`--calibration`／`--metrics`（`cli/show.py`；exit code 不變）。
- 事件：`record_added` 加 `gated` 欄位（既有必填欄位不動）。
- 測試：`tests/test_specs.py`（gates／wsum／預設不變、註冊驗證）、`tests/test_evaluation.py`（曲線單調、repeat 不計、校準 ρ 對已知資料、gate_pass_rate）、`tests/test_db.py`（索引新欄位向後相容：舊行無 `pred`／`gated` 仍讀得到）、`tests/test_cli.py`（三個旗標）。
- docs：`implementation.md` §3（索引欄位）、§4（Spec）、§8（report 旗標）、新 §5.8「評估層」；`architecture.md` D1 補一句「評估規則＝Spec（閘門＋聚合），策略預測只當附註由 runtime 校準」。
- commit：`feat: 評估層——Spec 閘門與聚合、效率曲線、策略校準、指標摘要（第三輪 M17）`

## M18　歷程與取樣：parent 真的有人填、算法自訂標籤、View 取樣

- `Proposal` 鍵集加 `tag: str|None`、`run: str|None`（`KEYS` 更新；`from_dict` 仍嚴格）；`parent` 保留單值，多親本放 `note["parents"]`（list）。`validate_proposals` 驗 `tag`／`run` 為 `is_valid_name` 或 None（進索引、要能當檔名段）。
- `Record` 加 `tag`、`run`；`INDEX_FIELDS` 加 `tag`、`run`；inflight `items` 帶 `tag`／`run`；`collect` 帶進 Record。`notarize` 重測沿用原筆 `tag`／`run`。
- `View` 加：`query(..., tag=None, run=None, parent=None)`；`children(rec_id)`；`lineage(rec_id, depth=10) -> list[Record]`（沿 `parent` 往上）；`sample(n, *, seed, **filters) -> list[Record]`（過濾後均勻抽、決定性）；`runs(strategy=None) -> list[str]`。`mine(run=…)`。
- 內建 `top_k_flip` 示範填 `parent`／`tag`（`flip-k<k>`）／`run`（seed）。
- `record_id` 不變（內容雜湊）；同一片 pattern 被兩個算法提出時只有先到者量、後者 `n_dup`——文件寫明「命名是標籤不是身分」。
- 測試：`tests/test_model.py`（鍵集）、`tests/test_strategy.py`（tag／run 驗證）、`tests/test_db.py`（新欄位、`children`／`lineage`／`sample` 決定性）、`tests/runtime/test_collect.py`（tag／run 落地）、`tests/test_strategies_shipped.py`（top_k_flip 填 parent）。
- docs：`implementation.md` §3、§4、§7（寫策略：怎麼標記自己的 pattern）；`naming.md`（tag／run 命名規則）。
- commit：`feat: 歷程與取樣——Proposal tag／run、索引欄位、View children／lineage／sample（第三輪 M18）`

## M19　算法託管：收件匣＋client（propose() 並存）

**收件匣＝同一個 Proposal 契約的第二種投遞方式；去重、派工、評分、公證全走既有路。**

- `paths.py` 新：`INBOX = "inbox/"`；`inbox_dir(profile, name)`＝`inbox/<profile>/<name>/`；`submission_file(profile, name, sid)`＝`…/<sid>.npz`（doc：`patterns` bool[n,*shape]、`meta` json 字串：`{name, run, at, items:[{parent, tag, note}]}`）；`submission_status(profile, name, sid)`＝`…/<sid>.status.json`（runtime 寫：`{state: received|dispatched|rejected, store, at, reason}`）；`algo_log(profile, name)`＝`…/log.jsonl`（**算法自己的單寫者日誌，平台永不讀**）；`layout_prefixes()` 加 `inbox/`。sid＝`<stamp>-<rand4>`。
- strategies.yaml 的策略項加 `kind: propose|inbox`（預設 `propose`）；`kind: inbox` 不需要 `.py`（`resolve_strategy_path` 跳過）、`batch`＝每 tick 最多收幾筆、`prio`／`max_inflight` 照舊（D5 的靜態配額對收件匣同樣生效）。
- `runtime/schedule.py`：`kind == inbox` 的策略不叫 propose，改 `inbox.take(rt, name, budget)`：列 `inbox_dir`、依 sid 排序（最舊先）、每個 submission 讀 → `validate_proposals`（不過→寫 `rejected` status＋`inbox_rejected` 事件，**不整批拒別的 submission**）→ 湊滿 `budget` 為止 → `dispatch(...)`（origin=`inbox`）→ 每個被收的 submission 寫 `dispatched` status（含 store）→ 刪 submission npz。崩在 dispatch 與刪檔之間 → 下 tick 重讀 → 去重（ids 在 inflight）→ `n_dup`，不重量。一個 submission 大於 `budget` → 拆到多個 tick（status 記 `dispatched_partial`＋剩餘 ids）。
- `emforge/client.py`（核心，DEPOT_ONLY）：`Client(depot, profile, name, *, run=None)`：
  - `submit(patterns, *, parents=None, tags=None, preds=None, notes=None) -> sid`（本機驗 shape／fixed_on 先擋，再 `put_bytes` 原子寫）；
  - `status(sid) -> dict`；`wait(sid, *, timeout_s, poll_s) -> dict`（到 `dispatched` 或 `rejected`）；
  - `results(sid) -> list[Record]`（由 status 的 store＋ids 對 db 查，未收齊回已收部分；`kind=sample`）；
  - `db -> View`（綁 `name`，`mine()`／`sample()`／`lineage()` 可用）；
  - `log(event, **fields)`（寫 `algo_log`；不進 `events.EVENTS` 白名單——這是算法的私事）。
  - 事件（平台側）：`inbox_received(name, sid, n)`、`inbox_rejected(name, sid, reason)`、`batch_dispatched` 的 `origin=inbox`。
- CLI：`emforge submit --profile P --name S --patterns file.npz [--run R]`（人或腳本投批）、`emforge inbox --profile P`（列各收件匣待收／已派）。`init` 的 strategies.yaml 範本加一個註解掉的 `kind: inbox` 例子。
- 示範託管算法 `emforge/examples/anneal_hosted.py`（不進核心 import 圖；`examples/` 進 LOCAL_LAYER 豁免）：模擬退火，用 `Client` 送批→`wait`→`results`→依自己的溫度／接受率決定下一批，`log()` 記自己的指標；`tests/test_e2e.py::test_hosted_anneal_loop_on_memory_depot`：runtime（in-process）＋`run_all_jobs`＋退火跑 3 輪，斷言每輪 submission 都 dispatched、results 對得上、`algo_log` 有 3 行、db 裡 `strategy="anneal"`／`run` 一致、report 列出 `anneal`。
- 守門：`test_smoke` 把 `client.py`、`runtime/inbox.py` 進 DEPOT_ONLY；`examples/` 豁免並註明。
- 測試：`tests/runtime/test_inbox.py`（收最舊先、配額、部分派、驗證失敗只拒那筆、崩在刪檔前不重量、`max_inflight`）、`tests/test_client.py`（submit 原子、status 轉換、results 部分可見、log 單寫者）、`tests/test_cli.py`（submit／inbox）、`tests/test_strategy.py`（yaml `kind` 解析與保留字）。
- docs：`implementation.md` §2（client／runtime/inbox／examples）、§3（inbox/ 佈局）、§5.3（schedule 兩種 kind）、§7 改名「寫一個策略／託管一個算法」、§8、§9、§12（收件匣的失效模式：submission 半截＝put 原子所以不會；配額餓死背景 inbox；同 name 多行程寫同一 log＝違反單寫者，name 要一算法一行程）；`architecture.md` 加 **D11：收件匣＝同一契約的第二種投遞；算法自評歸算法**。
- commit：`feat: 算法託管——收件匣（kind: inbox）＋client（submit／wait／results／db／log）＋退火示範（第三輪 M19）`

## M20　平台 MCP：資料庫（唯讀）、評估（唯讀）、收件匣（送批）

- `emforge/platform/mcp_server.py`（唯一另一個 import `mcp` 處，函式內）：`build_platform_server(depot, root, *, secret)`；重用 `device/mcp_server.py` 的 `BearerGate`／`check_bind`／`http_app` 骨架（抽到 `emforge/mcpkit.py`：bearer、bind 檢查、ToolError 轉換、兩段式 helper——device 與 platform 共用，函式內 import mcp）。
- tools（同步 def）：
  - 唯讀：`db_profiles()`、`db_query(profile, strategy=None, arm=None, status=None, since_tick=None, tag=None, run=None, limit=100)`（回 metas，不回 bits）、`db_top(profile, k)`、`db_sample(profile, n, seed, **filters)`、`db_lineage(profile, id, depth)`、`db_record(profile, id)`（含 bits 字串與 measure）、`report(profile)`、`report_curve(profile, strategy=None)`、`report_calibration(profile, strategy)`、`report_metrics(profile)`、`fleet()`；
  - 送批：`inbox_submit(profile, name, patterns: list[str], run=None, parents=None, tags=None, confirm=None)`——兩段式（token 機制沿用 `Instrument` 的 nonce 式，抽成 `mcpkit.Confirmer`）；`inbox_status(profile, name, sid)`。
  - 沒有任何寫榜、寫設定、清急停的 tool。
- resources：`emforge://profiles`、`emforge://report/<profile>`、`emforge://status/<profile>`。
- CLI：`emforge platform-serve --root R --depot D [--host --port]`（同 device-serve 的 token／bind 規則；`EMFORGE_PLATFORM_PORT` 預設 8766）；`fleet --mcp-config` 一併吐 platform 條目。
- 測試 `tests/platform/test_mcp_server.py`（`importorskip("mcp")`，in-memory Client）：tools 名單與 read-only hints、`db_query` 過濾、`db_sample` 決定性、`inbox_submit` 兩段式→runtime 一個 tick 後 dispatched、壞 bits → `bad_bits`、無 token 非 loopback 拒起、沒有 promote／clear 類 tool。
- docs：`implementation.md` §2、§8、§12（平台 MCP 的安全邊界：一把 token 同時開儀器與平台）、§13；`naming.md`（tools 名、`EMFORGE_PLATFORM_PORT`）；`deploy.md` 加 platform-serve 一段；`CLAUDE.md` mcp import 規則加 `platform/mcp_server.py`、`mcpkit.py`。
- commit：`feat: 平台 MCP——資料庫／評估唯讀 tools 與收件匣送批（第三輪 M20）`

## 不做（明列）
- runtime 管算法行程（supervisor）：託管＝收件匣，行程由算法自己或人管。
- 給 agent 改原始碼的 MCP tool：程式碼走 git＋測試＋人 commit（供應鏈邊界）。
- 通用評估框架／指標外掛系統：只有 Spec 欄位、`evaluation.py` 純函式、report 旗標。
- Record 格式遷移：不需要（measure dict 已完整）。
- 正式機驗收、remote：延後（Ricky）。

## 驗證
- 每里程碑：`python -m pytest -o addopts="" -q` 全綠（519 → 約 580）、`pyflakes`、smoke 守門（行數／依賴方向／mcp 函式內）。
- M17：對合成資料的效率曲線單調、repeat 不改 P 與曲線、校準對已知線性預測 ρ≈1。
- M18：`lineage` 對三代鏈回三筆；`sample` 同 seed 同結果。
- M19：`test_hosted_anneal_loop_on_memory_depot` 整條在 `memory://` 跑通；崩在刪 submission 前重讀不重量。
- M20：in-memory MCP 客戶端；`inbox_submit` 兩段式後 runtime tick 派出。
- 完成後：更新 `docs/review-2026-09-07.md` 無關；`docs/architecture-report` 短版補一段「第三輪」（之後再印）。
